import { htmlElements } from './mocks';

interface HtmlElement {
	tag: string;
	content: string[];
	properties: { property: string; value: string }[];
}

export const generateHtml = () => {
	return '';
};
